import 'chai/register-should';
import Mixer from '../src/mixer';

describe('mixer-js', async () => {
  describe('#init()', async () => {
    it('should accept a list of tracks and filters', async () => {
      true.should.equal(true);
    });
  });
});